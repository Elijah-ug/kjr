export const store = async () => {
  try {
    // const newAdmin = await prisma
    console.log("Hello world from admissions");
  } catch (error) {
    console.log("Error==>", error);
  }
};

export const index = async () => {
  try {
    // const newAdmin = await prisma
    console.log("Hello world, from admissions");
  } catch (error) {
    console.log("Error==>", error);
  }
};
